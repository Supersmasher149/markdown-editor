{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the main window. Every permission is justified in README.md under 'Tauri capabilities'. Notably absent: the filesystem, shell, and process plugins - all disk access goes through the explicit Rust commands in src/commands/files.rs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "dialog:allow-open",
    "dialog:allow-save",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "https://*" }, { "url": "http://*" }]
    },
    "opener:allow-reveal-item-in-dir"
  ]
}
